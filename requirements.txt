# Core dependencies (GPU-accelerated)
# For CUDA 12.1+ (NVIDIA GPU):
torch>=2.0.0
torchvision>=0.15.0
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# For CPU-only (no GPU):
# pip install torch torchvision

numpy>=1.24.0

# Game Boy emulator
pyboy>=1.6.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Input handling
keyboard>=0.13.5

# Optional: OCR per lettura testo schermo (dialoghi, menu, nomi Pokemon)
# Installa con: pip install easyocr
# NOTA: Prima esecuzione scarica modello (~100MB)
# easyocr>=1.7.0

# Optional: For parallel training (future feature)
# multiprocessing is built-in
# ray>=2.9.0  # Uncomment for distributed training
